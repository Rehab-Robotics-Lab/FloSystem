init_config:
    instances:
        - host: session-store
          port: 6379
          tags:
              - service:session-store
        - host: client-store
          port: 6379
          tags:
              - service:client-store
